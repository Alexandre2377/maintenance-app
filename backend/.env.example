# ================================
# AI API 설정 (필수)
# ================================

# Groq AI API Key (필수)
# https://console.groq.com 에서 무료로 발급 가능
GROQ_API_KEY=your-groq-api-key-here

# ================================
# 인증 설정 (필수)
# ================================

# JWT Secret Key (필수)
# 아래 명령어로 생성: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this-to-random-string

# JWT Algorithm (선택, 기본값: HS256)
ALGORITHM=HS256

# JWT Access Token 만료 시간 (분 단위, 선택, 기본값: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ================================
# 데이터베이스 설정 (선택)
# ================================

# Database URL (선택, 기본값: SQLite)
# SQLite: sqlite:///./maintenance.db
# PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./maintenance.db

# ================================
# Celery & Redis 설정 (선택)
# ================================

# Redis URL (Celery 작업 큐용, 선택, 기본값: localhost)
# 로컬: redis://localhost:6379/0
# Railway: redis://default:password@host:port
REDIS_URL=redis://localhost:6379/0

# ================================
# AWS S3 설정 (파일 업로드용, 선택)
# ================================

AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-northeast-2
S3_BUCKET_NAME=maintenance-files

# ================================
# 기타 설정
# ================================

# CORS 설정 (선택, 기본값: 모든 도메인 허용)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app

# 환경 (선택, 기본값: development)
ENVIRONMENT=development
